name: pr large files

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  guidance:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: bobleesj/github-bots/.github/workflows/_pr-large-files.yml@v0
